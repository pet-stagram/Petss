const admin = require("../services/adminService");
const auth = require("../services/authService");
const posts = require("../services/postsService");

// test('first test', () => {return posts.selectPostsAll(1)});